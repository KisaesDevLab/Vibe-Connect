{
  "$schema": "../../node_modules/@tauri-apps/cli/schemas/capability.schema.json",
  "identifier": "main-capability",
  "description": "Onboarding + thin-client capabilities. The webview loads arbitrary HTTPS appliance URLs after onboarding; the IPC surface is the five custom commands defined in main.rs (get/set/clear appliance URL, navigate_to_appliance, get_desktop_version) which auto-allow without an explicit permission entry. Plugin permissions below are kept to the defaults so a future feature can pull from them without revisiting this file.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "core:menu:default",
    "core:tray:default",
    "autostart:default",
    "global-shortcut:default",
    "notification:default",
    "store:default",
    "updater:default",
    {
      "identifier": "shell:allow-open",
      "allow": [{ "url": "https://**" }, { "url": "http://**" }]
    }
  ]
}
